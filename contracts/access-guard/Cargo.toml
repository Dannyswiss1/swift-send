[package]
name = "access-guard"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { version = "23.2.1", default-features = false, features = ["testutils"] }
